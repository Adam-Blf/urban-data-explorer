# Dependances runtime de l'API en mode local-first (Render).
# Les drivers DB/streaming (cassandra, kafka, scrapling) sont exclus : l'API
# calcule ses donnees sans conteneur. Voir requirements.txt pour la stack complete.
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
python-dotenv==1.0.1
polars==1.24.0
pyarrow==19.0.1
PyJWT==2.10.0
python-multipart==0.0.18
requests==2.32.3
# Imports top-level de l'API : observability.py (metrics) et db.py (via
# routers/tables.py). Sans eux l'app crashe avant de bind le port sur Render.
prometheus_client==0.21.1
psycopg2-binary==2.9.9
